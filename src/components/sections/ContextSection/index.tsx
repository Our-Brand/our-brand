import { ViewportOptions } from "@/types/ui";

type ContextSectionProps = {
  viewport: ViewportOptions;
};

const ContextSection = ({ viewport }: ContextSectionProps) => {
  return <></>;
};

export default ContextSection;
